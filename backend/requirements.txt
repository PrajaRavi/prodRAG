# --- Web Framework & API Core ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- LangChain & LangGraph Core ---
langchain>=1.0.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# --- LLM Providers (Uncomment the ones you use) ---
# langchain-openai>=0.2.0
# langchain-anthropic>=0.2.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langchain_nomic

# --- Vector Store & Embeddings (For RAG/Memory) ---
# langchain-chroma>=0.1.0
# faiss-cpu>=1.8.0
pinecone
langchain_pinecone

# --- Cloud and deployment ---
  imagekitio
# --- Database & Persistence ---
 asyncpg>=0.29.0       # For PostgreSQL state persistence in LangGraph
 psycopg-pool>=3.2.0
 langgraph-checkpoint-postgres

# --- Utilities & Performance ---
python-dotenv>=1.0.1
sse-starlette>=2.0.0    # Required for streaming responses (Server-Sent Events) to React
httpx>=0.27.0